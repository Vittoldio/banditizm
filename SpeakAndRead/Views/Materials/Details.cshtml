@model SpeakAndRead.Models.Material

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Material</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.MaterialName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.MaterialName)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Content)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Content)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Admin") || User.IsInRole("Teacher"))
    {
    <div class="btn btn-primary">

        <a asp-action="Edit" asp-route-id="@Model.MaterialId" style="color:whitesmoke">Edit</a>
    </div>
    }
    <div class="btn btn-secondary">

    <a asp-action="Index" style="color:whitesmoke">Back to list</a>
</div>
</div>
